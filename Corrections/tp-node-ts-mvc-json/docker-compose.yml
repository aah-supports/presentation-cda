services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tp-node-ts-mvc-json-api
    ports:
      - "3001:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
